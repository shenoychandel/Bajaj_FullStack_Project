# BFHL Frontend (Vite + React)

A minimal client to hit the backend `/bfhl` route and visualize the response.

## Run locally
```bash
cd frontend
npm install
npm run dev
# open http://localhost:5173
# set endpoint to your deployed backend URL ending with /bfhl
```
